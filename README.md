# indexHW
Indexing in Python is used to access individual elements of a string, list, tuple, or other sequence. Each element has a position called an index. Indexing starts from 0 for the first element. Negative indexing starts from -1 and is used to access elements from t
